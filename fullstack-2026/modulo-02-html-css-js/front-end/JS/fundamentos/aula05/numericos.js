/**
 * O Javascript é uma linguagem fracamente típada, isso significa que  
 * “ele” tentará converter em tempo de execução os dados no valor. 
 */

/**
 * podem ser classificados em dois grupos principais: tipos primitivos e não primitivos.
 * Primitivos → string, number, boolean, null, undefined, symbol, bigint
 * Não primitivos → object, array, function
 */
